# Core dependencies for RL Prisoner's Dilemma project
numpy>=1.21.0
matplotlib>=3.5.0
scipy>=1.7.0

# Reinforcement Learning libraries
gym>=0.21.0
gymnasium>=0.28.0  # Newer version of gym
stable-baselines3>=1.6.0

# Deep Learning (if using DQN or other neural network approaches)
torch>=1.12.0
tensorflow>=2.10.0  # Optional, uncomment if needed

# Utilities
tqdm>=4.64.0
pandas>=1.5.0
seaborn>=0.12.0

# Jupyter (for notebooks)
jupyter>=1.0.0
ipykernel>=6.15.0
ipython>=8.0.0  # For display, clear_output, HTML

# Image and video processing
imageio>=2.20.0
imageio-ffmpeg>=0.4.8  # Required for moviepy video processing
pillow>=9.0.0  # For PIL.Image
moviepy>=1.0.3  # For ImageSequenceClip

# Note: google.colab is only available in Google Colab environment
# and cannot be installed via pip

